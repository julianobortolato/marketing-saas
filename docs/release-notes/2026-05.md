# Marketing SaaS — Novidades de Maio/2026

## Para o Dono da Academia

O agente de WhatsApp passou por uma bateria completa de testes antes de entrar em contato com seus leads. Foram simuladas 25 situações diferentes — desde o primeiro "olá" de um lead até pedidos de desconto e transferência para atendimento humano — e todas funcionaram como esperado.

Também foi validado que o agente fala com a identidade da sua academia, não com a identidade de nenhuma outra. Isso significa que quando seu lead conversar com o CMO IA, vai sentir que está falando com a sua academia.

## Para o Gestor

- Smoke suite automatizada disponível para rodar antes de cada atualização do sistema
- Relatório de identidade (`.identity-leak-report.json`) gerado a cada execução — auditável
- Deploy de produção ativo em marketing-saas-nu.vercel.app
- **Novo:** quando você assume uma conversa, agora pode **responder o lead diretamente pelo painel** — um campo de texto aparece no rodapé da conversa enquanto a IA está pausada. Digite e pressione Enter para enviar via WhatsApp sem sair do dashboard.
- **Corrigido:** botão "Assumir conversa / Reativar IA" agora atualiza instantaneamente ao clicar, sem precisar recarregar a página.

## Bastidores (técnico — atualizado 26/mai)

- 25 smoke tests cobrindo webhook, kill switch de budget, handoff humano, idempotência e gate de contraste ENGINE_VS_TENANT
- 3 bugs corrigidos: seed trigger-aware, grants ai_usage_*, handoff pre-LLM
- SHA e216238
- Resposta manual: persist-before-send + Evolution API + status_envio rastreado (SHA 32192b1)
- Fix handoff toggle: useState otimista no Client Component
- Fix editorial save: upsert → update para colunas parciais
- Segurança de integração reforçada: sistema de autenticação do canal WhatsApp com múltiplos mecanismos de validação (SHA 319f543)
- Documentação técnica interna v1.0 do produto (arquitetura, domínio, roadmap, decisões de design) adicionada ao repositório (SHA 2e12be2)

---

## Para o Gestor — Atualização 27/mai (Fase 5)

O Prisma agora gera automaticamente os posts da semana toda segunda-feira às 8h da manhã, sem que você precise fazer nada. O sistema analisa o perfil da sua academia, escolhe a melhor foto disponível e cria o texto do post — tudo pronto para você aprovar com um clique.

Na tela de aprovações, cada post agora aparece com:
- A imagem final já montada (como vai aparecer no Instagram)
- O texto gerado
- As hashtags sugeridas
- Botões para aprovar, rejeitar ou editar o texto antes de aprovar

Você pode aprovar ou rejeitar cada post individualmente, ou aprovar tudo de uma vez.

Após aprovação, você recebe um email avisando que os posts estão prontos.

---

## Bastidores (técnico) — Fase 5 bugfix (27/mai, SHA f339db1)

- **Corrigido:** coluna `logo_url` estava ausente em `tenant_config` — o gerador de posts retornava erro silencioso e não gerava nada
- Coluna adicionada via migration idempotente (`IF NOT EXISTS`) — safe em banco de produção
- Diagnóstico melhorado: erros de configuração agora aparecem explicitamente no log de produção
- Causa raiz registrada como anti-padrão: schema assumido sem `SELECT * FROM tabela LIMIT 5` antes de implementar
