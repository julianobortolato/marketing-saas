---
phase: 1
slug: foundation
status: approved
shadcn_initialized: false
preset: none
created: 2026-05-19
reviewed_at: 2026-05-19
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for Phase 1: Foundation.
> Generated by gsd-ui-researcher. To be verified by gsd-ui-checker.
>
> Brand source of truth: CLAUDE.md § "Identidade visual" (Fitness UNIC identity).
> Stack: Next.js 14 App Router + TypeScript + Tailwind CSS + Supabase Auth.
> No code exists yet — this is a greenfield implementation.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn/ui (initialize before coding) | CLAUDE.md — modern sans-serif + Tailwind stack |
| Preset | Custom — configure via `npx shadcn init` with Tailwind CSS variables | default |
| Component library | Radix UI (via shadcn) | default for Next.js + Tailwind |
| Icon library | lucide-react | shadcn default, matches clean aesthetic |
| Font | Inter (variable, via `next/font/google`) | CLAUDE.md — "Inter ou Geist", Inter preferred |

**shadcn init note:** Run `npx shadcn@latest init` during Phase 1 setup. Use `Default` style, `Zinc` base color, CSS variables enabled. Then override CSS variables in `globals.css` with brand tokens listed in the Color section below.

---

## Spacing Scale

Declared values (all multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gap, badge internal padding |
| sm | 8px | Input internal padding vertical, compact list items |
| md | 16px | Default element spacing, card padding horizontal |
| lg | 24px | Card padding, form section gaps |
| xl | 32px | Layout column gaps, major block separation |
| 2xl | 48px | Auth page vertical centering padding |
| 3xl | 64px | Page-level top/bottom breathing room |

Exceptions:
- Touch targets on mobile (role badges, nav items): minimum 44px height — enforced via `min-h-[44px]`
- Auth card max-width: 400px centered — not a spacing token, a layout constraint

---

## Typography

Font family: `Inter, ui-sans-serif, system-ui` via `next/font/google` — loaded as CSS variable `--font-inter`, applied to `<html>`.

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 16px / `text-base` | 400 (regular) | 1.5 | Form labels, paragraph text, descriptions |
| Label | 14px / `text-sm` | 400 (regular) | 1.4 | Input labels, table headers, nav items, role badges |
| Heading | 20px / `text-xl` | 700 (bold) | 1.2 | Page titles (h1), card section titles (h2), form section headers |
| Display | 28px / `text-3xl` | 700 (bold) | 1.1 | Auth page headline only ("Seu CMO autônomo começa aqui") |

**Capitalization rule:** Headings in the dashboard use `uppercase tracking-wide` (Tailwind `uppercase tracking-wider`) — mirrors Fitness UNIC brand style ("títulos em maiúsculas bold"). Auth page display headline is sentence case only. Size difference (14px vs 16px) and uppercase transforms provide sufficient visual hierarchy between roles without additional weights.

---

## Color

Brand palette from CLAUDE.md § "Identidade visual":

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#FFFFFF` — `--color-bg` | Page background, auth card background, form backgrounds |
| Secondary (30%) | `#F8FAFC` — `--color-surface` | Dashboard layout sidebar, dashboard main content area, form input backgrounds; borders use `#E2E8F0` (`--color-border`) |
| Accent (10%) | `#E30613` — `--color-primary` | See reserved list below |
| Text primary | `#0F172A` — `--color-text` | All body copy, headings |
| Text muted | `#64748B` — `--color-muted` | Secondary labels, helper text, placeholder text |
| Destructive | `#DC2626` (Tailwind `red-600`) | Danger/delete confirmation dialog confirm button only |

**Accent `#E30613` reserved for exactly 3 uses:**
1. Primary CTA buttons (Criar conta, Entrar na conta, Salvar configurações) — filled background
2. Active nav item left-border indicator in sidebar
3. "Required field" asterisk (`*`) in academia DNA form

**Accent is NOT used for:**
- Page backgrounds or section fills
- Hover states on non-CTA elements
- Informational badges or status chips
- Error states (use `#DC2626` destructive instead)
- Valid-state input borders (use `#16A34A` green-600 for valid/success input outline)

**Tailwind CSS variable override** in `src/app/globals.css`:
```css
:root {
  --background: 0 0% 100%;              /* #FFFFFF */
  --foreground: 222 47% 11%;            /* #0F172A */
  --card: 210 40% 98%;                  /* #F8FAFC */
  --card-foreground: 222 47% 11%;
  --primary: 354 95% 46%;               /* #E30613 */
  --primary-foreground: 0 0% 100%;
  --muted: 210 40% 98%;
  --muted-foreground: 215 16% 47%;      /* #64748B */
  --border: 214 32% 91%;               /* #E2E8F0 */
  --destructive: 0 72% 51%;            /* #DC2626 */
  --destructive-foreground: 0 0% 100%;
}
```

---

## Screen Inventory — Phase 1

Phase 1 delivers 4 distinct screens:

| Route | Screen | Primary Action |
|-------|---------|----------------|
| `/signup` | Create account | Submit email + password |
| `/login` | Sign in | Submit email + password |
| `/dashboard/overview` | Dashboard hub | Navigate to config |
| `/dashboard/configuracoes` | Academia DNA form | Save DNA config |

### `/signup` — Create account

**Layout:** Centered card, 400px max-width, white background, `py-48` vertical centering on `#F8FAFC` page background.

**Elements (top to bottom):**
1. Logo mark — Fitness UNIC wordmark, centered, 32px height
2. Display heading: "Seu CMO autônomo começa aqui" — 28px bold, sentence case, `#0F172A`
3. Subheading: "Crie sua conta e configure sua academia em minutos." — 16px regular, `#64748B`
4. Email input — label "E-mail", type email, placeholder "voce@academia.com.br"
5. Password input — label "Senha", type password, placeholder "Mínimo 8 caracteres", toggle visibility icon
6. Primary CTA button — "Criar conta" — full-width, `#E30613` background, white text, 16px bold, 44px height
7. Divider: "Já tem conta?" + link "Entrar na conta" → `/login`

**States:**
- Loading: button shows spinner (lucide `Loader2` with `animate-spin`), disabled, text changes to "Criando conta..."
- Error inline: red helper text below field (`#DC2626`, 14px), not toast
- Success: redirect to `/dashboard/configuracoes` (first-time flow) or `/dashboard/overview`

### `/login` — Sign in

**Layout:** Same as signup — centered card 400px.

**Elements:**
1. Logo mark
2. Heading: "Bem-vindo de volta" — 20px bold, uppercase, `#0F172A`
3. Email input — same as signup
4. Password input — same as signup + "Esqueceu sua senha?" link (14px, `#64748B`) aligned right below input
5. Primary CTA button — "Entrar na conta" — full-width, `#E30613`, 44px height
6. Divider: "Ainda não tem conta?" + link "Criar conta" → `/signup`

**States:** Same loading/error pattern as signup.

### `/dashboard/overview` — Dashboard hub

**Layout:** App shell — left sidebar (240px, `#F8FAFC` background) + main content area (`#FFFFFF`).

**Sidebar elements:**
- Logo mark at top, 24px height, 16px horizontal padding
- Nav items (Phase 1 only shows active items):
  - "Visao Geral" — active indicator: 3px left border `#E30613`, background `#F1F5F9`
  - "Configuracoes" → `/dashboard/configuracoes`
  - Items for Leads, Conteudo, Campanhas, Inteligencia — present but visually muted (`#64748B` text, no click action — placeholder for future phases), labeled "(em breve)" in 12px `#94A3B8`
- Bottom: user avatar initial + email truncated + role badge

**Role badge styles:**
- `owner` — `#0F172A` background, white text, 12px regular uppercase
- `manager` — `#334155` background, white text
- `viewer` — `#F1F5F9` background, `#64748B` text, border `#E2E8F0`

**Main content area:**
- Top bar: page title "VISAO GERAL" — 20px bold uppercase, `#0F172A`; right side: tenant name
- Welcome card (shown when `academia_config` is empty): surface card `#F8FAFC`, border `#E2E8F0`, padding 24px
  - Heading: "Configure o DNA da sua academia" — 20px bold
  - Body: "Para que o CMO autônomo trabalhe por você, precisamos conhecer sua academia." — 16px regular `#64748B`
  - CTA: "Configurar academia" — `#E30613` button → `/dashboard/configuracoes`
- Summary cards row (shown when `academia_config` exists): 3 cards side-by-side, surface bg, 16px padding, 8px border-radius. Phase 1 cards show "0" counters as placeholders for Leads, Agendamentos, Conteudos.

### `/dashboard/configuracoes` — Academia DNA form

**Layout:** App shell (same sidebar) + scrollable main content, 720px max content width.

**Form structure — 4 sections with 24px gap between sections:**

**Section 1 — Identidade**
- "Nome da academia" — text input, required (`*`)
- "Bairro" — text input, required (`*`), helper: "Bairro principal onde sua academia opera"
- "Cidade" — text input, required (`*`)
- "Raio de atuacao (km)" — number input, default 5, min 1 max 50, suffix "km"

**Section 2 — Tom de voz**
- "Tom de voz" — radio group with 3 options:
  - "Formal" — "Comunicacao profissional e respeitosa"
  - "Neutro" — "Equilibrado, direto e claro" (default selected)
  - "Coloquial" — "Descontraido, proximo e motivador"
- Radio cards: 100% width, border `#E2E8F0`, selected state border `#E30613` + `#FFF5F5` background tint

**Section 3 — Diferenciais**
- "Diferenciais da academia" — tag input (user types, presses Enter to add chip). Chips: `#F1F5F9` bg, `#0F172A` text, `×` remove button. Helper: "Ex: Musculacao, Crossfit, Personal Trainer, Sauna, Estacionamento"
- Max 10 tags

**Section 4 — Horarios e Planos**
- "Horarios de funcionamento" — textarea, 4 rows, placeholder "Ex: Seg-Sex 06h-22h / Sab 08h-18h / Dom fechado"
- "Planos oferecidos" — textarea, 4 rows, placeholder "Ex: Mensal R$120, Trimestral R$300, Anual R$900"

**Form footer (sticky on mobile, static on desktop):**
- "Salvar configurações" — `#E30613` button, 160px min-width, right-aligned
- Loading state: "Salvando..." + spinner, disabled
- Success state: inline success message below button (not toast) "Configurações salvas com sucesso." — `#16A34A` (green-600), 14px, disappears after 3 seconds

---

## Interaction Contracts

### Form validation

- Validation fires on `onBlur` for each field (not on every keystroke)
- Required fields show error on blur if empty: red border (`#DC2626`), red helper text 14px below input
- Valid-state input border (on successful blur): `#16A34A` (green-600) outline — not accent red
- Form submit button disabled until all required fields (`*`) are non-empty
- No full-page validation summary — errors are inline, field-level only

### Loading states

- All async actions (signup, login, save config): button shows `Loader2` spinner + descriptive text, disabled, no other UI change
- Duration expectation: Supabase calls < 2s — no skeleton needed for Phase 1 forms

### Auth redirect rules

| Condition | Redirect |
|-----------|----------|
| Logged out, visits `/dashboard/*` | → `/login` |
| Logged in, visits `/login` or `/signup` | → `/dashboard/overview` |
| First signup (new tenant, no `academia_config`) | → `/dashboard/configuracoes` |
| Returning login (config exists) | → `/dashboard/overview` |

### Role-based UI rules

| Role | Can see | Cannot see |
|------|---------|------------|
| `owner` | All sections, all CTAs | — |
| `manager` | All sections, all CTAs | Billing fields (deferred to future phase) |
| `viewer` | All sections read-only | Save/submit buttons hidden (not disabled — hidden) |

Viewer enforcement: form fields use `readOnly` attribute, save button conditionally not rendered (`role !== 'viewer'`).

### Empty states

| Screen | Empty State |
|--------|-------------|
| `/dashboard/overview` — no DNA configured | Welcome card with "Configurar academia" CTA (see screen inventory above) |
| `/dashboard/overview` — DNA configured, no leads yet | 3 placeholder counter cards showing "0" with muted labels |
| `/dashboard/configuracoes` — first load | Pre-filled with defaults: raio_km = 5, tom_de_voz = "neutro" |

---

## Copywriting Contract

| Element | Copy | Notes |
|---------|------|-------|
| Signup primary CTA | "Criar conta" | Verb + noun, imperative |
| Login primary CTA | "Entrar na conta" | Verb + noun, imperative |
| DNA form save CTA | "Salvar configurações" | Verb + noun |
| Overview welcome CTA | "Configurar academia" | Verb + noun, imperative |
| Auth display headline | "Seu CMO autônomo começa aqui" | Aspirational, sentence case |
| Auth subheading | "Crie sua conta e configure sua academia em minutos." | Benefit-first |
| Signup loading | "Criando conta..." | Progressive gerund |
| Login loading | "Entrando..." | Progressive gerund |
| Save loading | "Salvando..." | Progressive gerund |
| Save success | "Configurações salvas com sucesso." | Declarative, green text inline |
| Required field error (empty) | "Campo obrigatório." | Short, unambiguous |
| Required field error (email format) | "Informe um e-mail válido." | Specific to error type |
| Password too short | "A senha deve ter pelo menos 8 caracteres." | — |
| Auth error (wrong credentials) | "E-mail ou senha incorretos. Verifique e tente novamente." | No specifics about which field is wrong (security) |
| Overview welcome heading | "Configure o DNA da sua academia" | Directive, outcome-oriented |
| Overview welcome body | "Para que o CMO autônomo trabalhe por você, precisamos conhecer sua academia." | Explains the why |
| Nav item future phases | "(em breve)" | Lowercase, parenthetical |
| Viewer read-only hint | No explicit hint in Phase 1 — viewer sees the form read-only, no action available | Simplest approach for MVP |

**Tone:** Brazilian Portuguese throughout. Direct, confident, no marketing fluff inside the app. Error messages explain the problem and the next step.

**Accents:** Use correct Portuguese accents in UI copy (autônomo, configurações, etc.) even though code variables/slugs are unaccented. The copy table above shows the canonical in-app strings.

---

## Component Inventory

Components needed for Phase 1 — install via `npx shadcn@latest add {component}`:

| Component | shadcn slug | Usage |
|-----------|-------------|-------|
| Button | `button` | All CTAs |
| Input | `input` | Email, password, text, number inputs |
| Label | `label` | All form labels |
| Card | `card` | Auth card, overview cards, welcome card |
| Separator | `separator` | Auth dividers |
| Badge | `badge` | Role badges |
| RadioGroup | `radio-group` | Tom de voz selector |
| Textarea | `textarea` | Horarios, planos fields |
| Avatar | `avatar` | User avatar in sidebar |

Custom components (not in shadcn, built from scratch):
- `TagInput` — chip/tag input for diferenciais field (wraps shadcn Input)
- `AppShell` — sidebar + main layout wrapper (Server Component)
- `NavItem` — sidebar navigation item with active state
- `PasswordInput` — shadcn Input + toggle visibility button (lucide `Eye` / `EyeOff`)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | button, input, label, card, separator, badge, radio-group, textarea, avatar | not required — official registry |
| Third-party | none declared | not applicable |

No third-party registries are used in Phase 1.

---

## Accessibility Minimums

- All form inputs have associated `<label>` via `htmlFor` / `id` (not `aria-label` only)
- Error messages linked to inputs via `aria-describedby`
- Password toggle button has `aria-label="Mostrar senha"` / `"Ocultar senha"`
- Color alone never conveys state — error inputs have both red border AND error text
- Nav items have `aria-current="page"` when active
- All interactive elements keyboard-navigable (shadcn components satisfy this via Radix)

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
