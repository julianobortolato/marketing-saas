---
phase: 03-agente-whatsapp
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - tests/smoke/webhook-evolution.smoke.test.ts
  - tests/smoke/rls-inverse.smoke.test.ts
  - tests/smoke/kill-switch.smoke.test.ts
  - tests/smoke/handoff.smoke.test.ts
  - tests/smoke/idempotency.smoke.test.ts
  - tests/smoke/hmac.smoke.test.ts
  - tests/smoke/new-lead.smoke.test.ts
  - tests/smoke/identity-leak.smoke.test.ts
  - tests/smoke/helpers/seed.ts
  - tests/smoke/helpers/webhook-payload.ts
  - tests/smoke/helpers/sign.ts
  - tests/smoke/README.md
  - package.json
  - .planning/phases/03-agente-whatsapp/CUTOVER-CHECKLIST.md
autonomous: false
requirements:
  - WHATS-01
  - WHATS-02
  - WHATS-03
user_setup:
  - service: evolution-api
    why: "Final cutover requires pointing the iara_v2_staging instance webhook URL at the new endpoint — manual via Evolution dashboard"
    dashboard_config:
      - task: "Update webhook URL for instance iara_v2_staging to https://<prod-domain>/api/webhooks/evolution"
        location: "Evolution API dashboard -> Instances -> iara_v2_staging -> Webhook settings"
  - service: supabase
    why: "Seed the Fitness UNIC tenant + evolution_instances row before cutover; seed the fictitious 'Academia Premium Vértice' for the anti-leak gate"
    dashboard_config:
      - task: "Insert evolution_instances row for Fitness UNIC (instance_name='iara_v2_staging', webhook_secret matching EVOLUTION_WEBHOOK_SECRET, ativo=true). Insert second evolution_instances row for fictitious tenant 'Academia Premium Vértice' with formal navy/white tema and senhor/senhora treatment in academia_config + caderno_editorial."
        location: "Supabase SQL editor — execute pre-flight SQL from CUTOVER-CHECKLIST.md"

must_haves:
  truths:
    - "Smoke test suite for webhook + RLS + kill switch + handoff + idempotency + HMAC + new-lead all pass against the deployed Vercel preview"
    - "RLS inverse suite: cross-tenant SELECT/INSERT/UPDATE/DELETE on evolution_instances, conversas, chat_messages, ai_usage_log, ai_usage_diario returns 0 rows / errors as expected"
    - "Anti-leak gate: 10 test conversations against the fictitious 'Academia Premium Vértice' tenant produce ZERO messages with Fitness UNIC tone/vocabulary leakage — manually scored by owner"
    - "Cutover checklist exists and the owner has gated each of: deploy + Vercel env vars verified + Supabase RLS suite green + anti-leak gate green + Evolution webhook URL switched"
    - "After cutover, the iara_v2_staging Evolution instance webhook hits /api/webhooks/evolution and a real inbound lead message produces an agent reply within 30s"
  artifacts:
    - path: "tests/smoke/webhook-evolution.smoke.test.ts"
      provides: "End-to-end smoke: valid HMAC + known instance + new message → 200 + DB rows present"
    - path: "tests/smoke/rls-inverse.smoke.test.ts"
      provides: "Cross-tenant access tests for 5 Phase 3 tables"
    - path: "tests/smoke/kill-switch.smoke.test.ts"
      provides: "Force ia_limite_diario_usd=0.01 → second call hits kill switch → ia_habilitada=false"
    - path: "tests/smoke/handoff.smoke.test.ts"
      provides: "Input 'desconto' → conversas.ia_ativa=false"
    - path: "tests/smoke/idempotency.smoke.test.ts"
      provides: "Redeliver same evolution_message_id 3x → 1 chat_messages row, 1 OpenAI call"
    - path: "tests/smoke/hmac.smoke.test.ts"
      provides: "Invalid HMAC → 401, nothing persisted"
    - path: "tests/smoke/new-lead.smoke.test.ts"
      provides: "First-ever message from unknown remotejid → leads + conversas + chat_messages rows created"
    - path: "tests/smoke/identity-leak.smoke.test.ts"
      provides: "10 scripted turns against 'Academia Premium Vértice' tenant; manual scoring rubric included"
    - path: "tests/smoke/helpers/seed.ts"
      provides: "seedTestTenant, seedFictitiousTenant, cleanupTestTenant helpers"
    - path: "tests/smoke/helpers/webhook-payload.ts"
      provides: "buildEvolutionWebhookPayload helper matching Evolution V2 shape"
    - path: "tests/smoke/helpers/sign.ts"
      provides: "signHmac(body, secret) helper using node:crypto"
    - path: ".planning/phases/03-agente-whatsapp/CUTOVER-CHECKLIST.md"
      provides: "Operational checklist with 10% → 50% → 100% gates + rollback procedure"
  key_links:
    - from: "tests/smoke/*.smoke.test.ts"
      to: "/api/webhooks/evolution"
      via: "fetch() against deployed Vercel preview URL"
      pattern: "fetch.*\\/api\\/webhooks\\/evolution"
    - from: "tests/smoke/rls-inverse.smoke.test.ts"
      to: "supabase JS client with anon key + spoofed JWT"
      via: "createClient with tenant A's JWT, attempt query for tenant B's row"
      pattern: "auth\\.setSession|setAuth"
    - from: "CUTOVER-CHECKLIST.md"
      to: "Evolution dashboard webhook URL switch"
      via: "manual step gated by checklist signoff"
      pattern: "iara_v2_staging"
---

<objective>
Deliver the verification + cutover vertical slice: a complete smoke test suite (ADR §11) that proves the agent slice (Plan 03-01) + observability/handoff slice (Plan 03-02) work end-to-end against deployed infrastructure, an explicit anti-leak gate that catches silent tenant-identity leakage in the LLM prompt, and a gated cutover checklist that progressively moves the Fitness UNIC Evolution instance from IARA V2 (current production) to the new /api/webhooks/evolution endpoint.

Purpose: Without this plan, Plans 03-01 and 03-02 are unverified code. The smoke suite is the ADR §11 gate. The anti-leak gate is the CLAUDE.md identity-rule enforcement at runtime (not just at lint time). The cutover checklist is what makes WHATS-01 actually live for the first real tenant.

Output: 8 smoke test files + 3 helpers + a README + a cutover checklist Markdown that the owner walks through one gate at a time.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agente-whatsapp/03-CONTEXT.md
@.planning/phases/03-agente-whatsapp/03-PATTERNS.md
@.adrs/ADR-MKT-001-agente-whatsapp.md
@CLAUDE.md

# Files produced by Plans 03-01 and 03-02 — smoke tests exercise these
@app/api/webhooks/evolution/route.ts
@lib/openai/client.ts
@lib/agents/cmo/guardrails.ts
@app/dashboard/conversas/actions.ts

# Migrations to reference for RLS suite
@supabase/migrations/20260520000007_create_whatsapp_tables.sql
@supabase/migrations/20260520000008_tenants_ia_budget.sql
@supabase/migrations/20260520000009_ai_usage_tables.sql
@supabase/migrations/20260520000010_whatsapp_functions.sql

# Existing webhook test analog (if present)
@app/api/webhooks/leads/route.ts
@lib/webhooks/verify-signature.ts

<interfaces>
Smoke test runner: detect at install time from package.json. Prefer the runner already used by Plans 03-01 and 03-02 unit tests. If none configured, use vitest (lightweight, native TS). The "smoke" directory uses .smoke.test.ts suffix to distinguish from fast unit tests — npm script "test:smoke" runs only this suite.

Smoke tests target a DEPLOYED Vercel preview (env var SMOKE_BASE_URL) — not localhost — to exercise real Vercel runtime, real Supabase connection, real Upstash, real OpenAI (with kill switch budget=0.10 USD for the test tenants).

Smoke tests are NOT in CI by default — they are run manually before cutover (script: npm run test:smoke). The reason: they consume real OpenAI tokens and real DB writes against a dedicated test tenant.

Required env vars for the smoke runner (read from .env.smoke, NEVER committed):
- SMOKE_BASE_URL=https://<preview-deployment>.vercel.app
- SMOKE_SUPABASE_URL
- SMOKE_SERVICE_ROLE_KEY (for seed/cleanup helpers)
- SMOKE_TEST_TENANT_ID (UUID of the test tenant seeded by seed.ts)
- SMOKE_FICTITIOUS_TENANT_ID (UUID of Academia Premium Vértice)
- SMOKE_EVOLUTION_WEBHOOK_SECRET (matches the value on the preview deployment)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke test helpers (seed, payload, sign) + ADR §11 test files (excluding identity-leak)</name>
  <files>tests/smoke/helpers/seed.ts, tests/smoke/helpers/webhook-payload.ts, tests/smoke/helpers/sign.ts, tests/smoke/webhook-evolution.smoke.test.ts, tests/smoke/rls-inverse.smoke.test.ts, tests/smoke/kill-switch.smoke.test.ts, tests/smoke/handoff.smoke.test.ts, tests/smoke/idempotency.smoke.test.ts, tests/smoke/hmac.smoke.test.ts, tests/smoke/new-lead.smoke.test.ts, tests/smoke/README.md, package.json</files>
  <read_first>
    - .adrs/ADR-MKT-001-agente-whatsapp.md (§11 full smoke test list — 8 items)
    - .planning/phases/03-agente-whatsapp/03-CONTEXT.md (Specifics block — ADR §11 required smoke tests)
    - app/api/webhooks/evolution/route.ts (Plan 03-01 output — exact response shapes for each gate)
    - lib/webhooks/verify-signature.ts (HMAC algorithm — reuse in helpers/sign.ts via direct node:crypto rather than importing app code into tests)
    - supabase/migrations/20260520000007_create_whatsapp_tables.sql (table column names for seed + cleanup)
    - supabase/migrations/20260520000008_tenants_ia_budget.sql (tenants budget columns for kill switch test)
    - supabase/migrations/20260520000010_whatsapp_functions.sql (RPC signatures called by the webhook)
    - app/api/webhooks/leads/route.ts (existing webhook — payload field names give us a model for what Evolution V2 webhook payloads look like in this project's expected shape)
    - package.json (existing scripts and devDependencies — add test:smoke script; install vitest + dotenv if not present)
    - CLAUDE.md (security rules — smoke tests still must not log lead PII; cleanup helpers run with service_role but cleanup is scoped to the test tenant UUIDs)
  </read_first>
  <action>
    Create tests/smoke/helpers/sign.ts exporting signHmac(rawBody: string, secret: string): string that returns 'sha256=' + createHmac('sha256', secret).update(rawBody).digest('hex'). Pure function, no imports from app code.

    Create tests/smoke/helpers/webhook-payload.ts exporting buildEvolutionWebhookPayload(opts: { instanceName: string; remotejid: string; messageId: string; text: string; pushName?: string }) returning the Evolution V2 webhook shape that app/api/webhooks/evolution/route.ts parses. The exact shape MUST match what Plan 03-01 implementation expects — read route.ts step 4 (payload parsing) to align field names.

    Create tests/smoke/helpers/seed.ts using createClient(SMOKE_SUPABASE_URL, SMOKE_SERVICE_ROLE_KEY). Export:
    - seedTestTenant(): inserts a tenant (iara_tenant_id=null, ia_habilitada=true, ia_limite_diario_usd=0.10 for safety), academia_config row with minimal valid editorial, evolution_instances row with instance_name='smoke_test_instance' and a known webhook_secret. Returns { tenantId, conversaId?: undefined } and stores tenantId in a singleton for cleanup.
    - cleanupTestTenant(tenantId): deletes chat_messages, conversas, ai_usage_log, ai_usage_diario, evolution_instances, academia_config, leads (in FK-safe order), then the tenant.
    - resetDailyUsage(tenantId): truncates ai_usage_diario for the tenant + sets ia_habilitada=true on the tenant.
    - forceKillSwitchBudget(tenantId, limitUsd: number): updates tenants.ia_limite_diario_usd.
    - getConversaIaAtiva(remotejid, tenantId): reads conversas.ia_ativa.
    - countChatMessages(tenantId, evolutionMessageId): returns COUNT(*) for idempotency check.

    Add to package.json: "test:smoke": "vitest run --config vitest.smoke.config.ts tests/smoke" script. Install vitest, dotenv if not present.

    Create vitest.smoke.config.ts at project root: loads .env.smoke via dotenv, sets testTimeout: 30_000, sequence: { concurrent: false } (smoke tests are stateful — must run sequentially), include: ['tests/smoke/**/*.smoke.test.ts'].

    Create tests/smoke/README.md documenting:
    - When to run (manually before cutover, not in CI)
    - Required .env.smoke contents (one var per line, example shape, never commit)
    - How to provision the test tenants (run a seed SQL or call seedTestTenant from a script — preferred: a one-off `npm run smoke:seed` script that calls seedTestTenant and prints the resulting tenant_ids for the operator to paste into .env.smoke)
    - Cost expectation: ~$0.10–$0.50 in OpenAI tokens per full run
    - Cleanup procedure if tests crash mid-run (call cleanupTestTenant(tenantId) directly)

    Create 7 smoke test files, each a vitest describe block that exercises ONE ADR §11 scenario:

    tests/smoke/hmac.smoke.test.ts:
    - Setup: build payload for SMOKE_TEST_TENANT_ID's instance
    - POST to ${SMOKE_BASE_URL}/api/webhooks/evolution with header 'x-hub-signature-256: sha256=deadbeef' (invalid)
    - Expect: status === 401, body.error === 'invalid_signature'
    - Expect: countChatMessages(tenantId, messageId) === 0 (nothing persisted)

    tests/smoke/webhook-evolution.smoke.test.ts:
    - Setup: build valid payload, sign with helpers/sign.ts using SMOKE_EVOLUTION_WEBHOOK_SECRET
    - POST to /api/webhooks/evolution
    - Expect: status === 200
    - Expect: countChatMessages === 1 within 5s polling window (webhook is synchronous, should be immediate)
    - Expect: a saida chat_message also exists within 30s (agent response)

    tests/smoke/new-lead.smoke.test.ts:
    - Setup: brand-new remotejid not present in leads
    - POST signed payload
    - Expect: 200; new row in leads (with origem='whatsapp'), new conversas row, new chat_messages row, all linked

    tests/smoke/idempotency.smoke.test.ts:
    - POST the same signed payload (same evolution_message_id) 3 times
    - Expect: each call returns 200
    - Expect: countChatMessages(tenantId, messageId) === 1 (deduplicated by UNIQUE constraint)
    - Expect: ai_usage_log has exactly 1 row for the corresponding conversa within the test window

    tests/smoke/handoff.smoke.test.ts:
    - POST signed payload with text 'oi, vocês têm desconto?'
    - Expect: 200
    - Expect within 30s: getConversaIaAtiva returns false
    - Expect: a saida chat_message exists (the guardrails fallback or handoff goodbye)

    tests/smoke/kill-switch.smoke.test.ts:
    - Setup: resetDailyUsage + forceKillSwitchBudget(tenantId, 0.01)
    - POST signed payload #1 → expect 200, agent runs, exceeds 0.01 USD on the single turn
    - Wait 3s for ai_usage_diario trigger
    - POST signed payload #2 with NEW message id → expect 200, but response is the fallback 'breve um atendente' (not LLM-generated)
    - Read tenants.ia_habilitada → expect false
    - Teardown: resetDailyUsage + forceKillSwitchBudget(tenantId, 0.10) to leave tenant usable for next test run

    tests/smoke/rls-inverse.smoke.test.ts:
    - Setup: seedTestTenant returns tenantA; seed a second isolated tenantB via a sibling seedTestTenant call (or a dedicated seedSecondTenant). Acquire a user session for tenantA (sign in via supabase.auth.signInWithPassword using a pre-seeded user OR mint a JWT via service_role).
    - Using the tenantA session client, attempt:
      - SELECT * FROM conversas WHERE tenant_id = tenantB → expect 0 rows
      - INSERT INTO chat_messages with tenant_id=tenantB → expect error (RESTRICTIVE block)
      - UPDATE conversas SET ia_ativa=false WHERE tenant_id=tenantB → expect 0 rows affected
      - DELETE FROM evolution_instances WHERE tenant_id=tenantB → expect 0 rows affected
    - Repeat for ai_usage_log and ai_usage_diario
    - Teardown: cleanupTestTenant(tenantB)

    Each test file: import { beforeAll, afterAll, describe, expect, it } from 'vitest'; load env via dotenv.config({ path: '.env.smoke' }) at module top. Each test file responsible for its own seed/teardown via the helpers (do NOT share state across files).

    For each smoke test, console output must NOT include lead conteudo or remotejid — log only tenantId, messageId, and assertion result.
  </action>
  <verify>
    <automated>npm run test:smoke -- --reporter=verbose</automated>
  </verify>
  <done>
    - 3 helpers + 7 smoke test files + README + vitest config + package.json script all exist
    - npm run test:smoke exits 0 with all 7 smoke describe blocks passing (run against a Vercel preview deployment)
    - Output of failed assertions does not leak conteudo or remotejid (grep -E 'remotejid|conteudo|telefone' on stdout returns 0 lines outside structured assertion error formatting)
    - vitest.smoke.config.ts sets sequence.concurrent=false (tests run sequentially)
    - .env.smoke is in .gitignore (grep '.env.smoke' .gitignore returns a match)
    - cleanupTestTenant runs in afterAll of each file — verify second run from clean state still passes (idempotent harness)
  </done>
</task>

<task type="auto">
  <name>Task 2: Anti-leak gate smoke — 'Academia Premium Vértice' fictitious tenant scoring</name>
  <files>tests/smoke/identity-leak.smoke.test.ts, tests/smoke/helpers/seed.ts</files>
  <read_first>
    - .planning/phases/03-agente-whatsapp/03-CONTEXT.md (Specifics: anti-leak gate — fictitious tenant 'Academia Premium Vértice', formal tone, navy/white, senhor/senhora treatment, 10 test conversations, ZERO UNIC-tone messages allowed)
    - .adrs/ADR-MKT-001-agente-whatsapp.md (§11 final bullet — anti-leak gate)
    - CLAUDE.md (Identidade visual section — engine-vs-tenant principle, prohibited hardcoded tenant strings)
    - lib/agents/cmo/system-prompt.ts (Plan 03-01 — verify the system prompt builder is fully driven by academia_config; this test PROVES that empirically)
    - lib/agents/cmo/guardrails.ts (Plan 03-01 — identity guardrail should also help here)
    - supabase/migrations/20260520000006_academia_config_fase3.sql (caderno_editorial column names for the formal Vértice config)
    - tests/smoke/helpers/seed.ts (Task 1 output — extend with seedFictitiousTenant)
  </read_first>
  <action>
    Extend tests/smoke/helpers/seed.ts with seedFictitiousTenant() that inserts:
    - tenants row { nome: 'Academia Premium Vértice', iara_tenant_id: null, ia_habilitada: true, ia_limite_diario_usd: 0.50, plano: 'pro' }
    - academia_config row { nome_academia: 'Academia Premium Vértice', bairro: 'Centro', cidade: 'São Paulo', tom_de_voz: 'Formal, técnico, tratamento senhor/senhora. Sem coloquialismos. Sem emojis em mensagens pós-aceite. Vocabulário sofisticado.', diferenciais: ['Equipamentos premium','Avaliação física com biomédico','Personal trainer pós-graduado','Sala climatizada'], horarios: { segunda_sexta: '06:00-22:00', sabado: '08:00-14:00' }, planos: [{ nome: 'Premium', valor: 599, modalidades: ['Musculação','Pilates','Avaliação']}], caderno_editorial_escopo: 'Comunicação técnico-consultiva. Foco em saúde longevidade e performance.', caderno_editorial_tom: 'Formal. Trate sempre como senhor/senhora. Evite gírias, emojis, exclamações. Use vocabulário sofisticado.', caderno_editorial_restricoes: 'Nunca usar bora, beleza, top, sensacional, massa, demais. Nunca usar emojis. Não confraternizar.', caderno_editorial_objetivos: ['Qualificar lead premium','Agendar avaliação física presencial','Demonstrar credibilidade técnica'], caderno_editorial_exemplos: 'Bem recebido. Posso compreender melhor seus objetivos? Em que posso auxiliá-lo(a)?', palavras_proibidas: ['bora','beleza','top','sensacional','massa','demais','vibe','rolê'] }
    - evolution_instances row with instance_name='smoke_vertice_instance' and webhook_secret matching .env.smoke value

    Create tests/smoke/identity-leak.smoke.test.ts. Strategy:
    - beforeAll: seedFictitiousTenant → SMOKE_FICTITIOUS_TENANT_ID
    - Define LEAD_SCRIPT: 10 distinct lead first-message scenarios covering different intents (curiosity, price, schedule, modality, recommendation, etc.) — each one a string in PT-BR with varying levels of formality from the lead.
    - For each scenario: POST a signed Evolution webhook payload to /api/webhooks/evolution; wait for the saida chat_message to be persisted (poll up to 30s); collect the agent response text.
    - Apply two layers of scoring:
      1. Hard regex gate (must auto-pass): assert response does NOT match /\b(bora|beleza|top|sensacional|massa|demais|vibe|rolê)\b/i. Use this as the test assertion — any match fails the test.
      2. Soft semantic gate (logged for manual review): for each response, write to a file tests/smoke/.identity-leak-report.json containing { scenario_index, lead_message, agent_response, hard_gate_pass: boolean }. Print a summary table to stdout at the end.
    - The test asserts: ALL 10 hard gates pass. afterAll: cleanupTestTenant(SMOKE_FICTITIOUS_TENANT_ID). Print "MANUAL REVIEW REQUIRED: open tests/smoke/.identity-leak-report.json and confirm tone matches Academia Premium Vértice formal style — no Fitness UNIC vocabulary leak even if hard gate passed."
    - Add tests/smoke/.identity-leak-report.json to .gitignore (small file containing test responses — not for repo).
  </action>
  <verify>
    <automated>npm run test:smoke -- tests/smoke/identity-leak.smoke.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - tests/smoke/identity-leak.smoke.test.ts exists and runs 10 scenarios
    - Hard gate passes (test exits 0)
    - tests/smoke/.identity-leak-report.json generated with 10 entries
    - Stdout prints "MANUAL REVIEW REQUIRED" notice with the report path
    - .gitignore contains tests/smoke/.identity-leak-report.json
    - seedFictitiousTenant inserts a tenant whose configuration contains ZERO references to Fitness UNIC, fitnessacademia, or red/#E30613 (grep over seed.ts confirms only formal-Vértice strings)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner reviews identity-leak report — semantic confirmation of zero tone leak</name>
  <what-built>
    - 10 agent responses to fictitious Vértice tenant captured in tests/smoke/.identity-leak-report.json
    - Hard regex gate already passed (no banned coloquial vocab)
  </what-built>
  <how-to-verify>
    1. Open tests/smoke/.identity-leak-report.json
    2. For each of the 10 entries, read agent_response and judge:
       a. Does it use formal Portuguese (senhor/senhora treatment OR neutral 'você' without slang)? — must be yes
       b. Does it avoid emojis except in places consistent with formal Brazilian business communication (greeting line emoji acceptable; sprayed emojis not)? — must be conservative
       c. Does it sound like ANY UNIC-vocabulary leak (mentioning bairros, modalities, or phrasing specific to Fitness UNIC's caderno editorial)? — must be NO
       d. Does the offer/AE proposal match Vértice's planos (Premium R$599, avaliação física) and NOT mention UNIC's modalidades or pricing?
    3. If all 10 entries pass all 4 sub-checks → approve.
    4. If 1+ entry fails: do NOT approve. Note which entries failed and on which sub-check. The cause is most likely an incomplete system prompt (e.g. buildSystemPrompt is reading a wrong column or dropping a block). Open a revision plan to Plan 03-01 before proceeding to cutover.
  </how-to-verify>
  <resume-signal>Type "approved" if all 10 entries pass semantic review, or "revise" + describe which entries failed and why</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Cutover checklist + pre-flight SQL + rollback script</name>
  <files>.planning/phases/03-agente-whatsapp/CUTOVER-CHECKLIST.md</files>
  <read_first>
    - .adrs/ADR-MKT-001-agente-whatsapp.md (§15 Sprints 4 items 16-18, §14 gatilho de revisita)
    - .planning/phases/03-agente-whatsapp/03-CONTEXT.md (Specifics: cutover protocol — current instance iara_v2_staging still pointing to IARA V2; manual switch after owner approval)
    - CLAUDE.md (checklist antes de push)
    - .planning/STATE.md (current project state — what tenants exist)
    - app/api/admin/saude-mkt/route.ts (Plan 03-02 — owner uses this endpoint to monitor each cutover stage)
  </read_first>
  <action>
    Create .planning/phases/03-agente-whatsapp/CUTOVER-CHECKLIST.md as a sequential gated checklist. Sections:

    ## Pre-flight (do these BEFORE switching the Evolution webhook URL)

    Each item is a checkbox the owner ticks as they complete it. Each item has an "expected outcome" and a "rollback" note where applicable.

    1. Deploy Plans 03-01 and 03-02 to Vercel production
       - Expected: latest commit SHA visible in Vercel deployment list, build green
       - Rollback: re-deploy previous commit from Vercel UI

    2. Verify Vercel env vars present in Production: EVOLUTION_WEBHOOK_SECRET, UPSTASH_REDIS_URL, UPSTASH_REDIS_TOKEN, OPENAI_API_KEY, SUPABASE_SERVICE_ROLE_KEY
       - Expected: vercel env ls shows all 5 with "Production" scope
       - Rollback: add missing var, re-deploy

    3. Run smoke suite against the production URL (npm run test:smoke with SMOKE_BASE_URL=production)
       - Expected: all 7 ADR §11 smoke tests pass
       - Rollback: do NOT proceed to cutover; debug failures

    4. Seed Fitness UNIC tenant evolution_instances row (SQL provided below)
       - Expected: select * from evolution_instances where instance_name='iara_v2_staging' shows 1 row with ativo=true and webhook_secret matching EVOLUTION_WEBHOOK_SECRET env var
       - Rollback: delete from evolution_instances where instance_name='iara_v2_staging'

    5. Confirm Fitness UNIC tenant has iara_tenant_id IS NULL (otherwise Plan 03-01 webhook routes to IARA bridge, not the agent)
       - Expected: select id, nome, iara_tenant_id from tenants where slug='fitness-unic' shows iara_tenant_id IS NULL
       - Rollback: set iara_tenant_id back to its previous value

    6. Owner reviews tests/smoke/.identity-leak-report.json from Task 2 — semantic approval
       - Expected: 10/10 hard + soft gates passed (Task 3 checkpoint)
       - Rollback: do not proceed; revise Plan 03-01 system prompt

    ## Cutover Stage 1: 10% traffic (1 hour observation window)

    7. In Evolution dashboard, configure iara_v2_staging webhook to route 10% of inbound to /api/webhooks/evolution and 90% to IARA V2. If Evolution does not support traffic splitting natively, simulate via a manual routing rule OR proceed to stage 2 directly with a small lead-volume window (single new lead test).
       - Expected: within 1h, at least 1 new inbound lead processed by the new endpoint
       - Monitoring: refresh /api/admin/saude-mkt every 10 min — usage_diario.custo_usd should track expected ~$0.03/turn; conversas.em_handoff stays at baseline rate
       - Rollback: revert Evolution webhook URL to IARA V2 only

    8. Owner reviews the agent's first 5 real-lead replies in /dashboard/conversas
       - Expected: tone matches UNIC caderno editorial; no banned words; AE proposal sensible
       - Rollback: same as step 7

    ## Cutover Stage 2: 50% traffic (24 hour window)

    9. Update Evolution routing to 50/50 split
       - Monitoring: same — refresh saude-mkt every hour; watch for kill switch trigger; verify p95 webhook < 30s
       - Rollback: revert to stage 1 (10%) or stage 0 (0%)

    10. Owner spot-checks /dashboard/conversas daily — confirm handoff rate < 30% and agendamento (status='agendado') rate > 20%
        - Expected: at least 1 AE booked in 24h via agendar_aula_experimental tool
        - Rollback: revert routing

    ## Cutover Stage 3: 100% traffic

    11. Update Evolution routing to 100% to /api/webhooks/evolution
        - Expected: IARA V2 stops receiving inbound; all leads handled by new agent
        - Rollback: revert to 50% or 10% routing in Evolution dashboard

    12. Run final smoke suite against production
        - Expected: all 7 ADR §11 smokes still pass

    13. Update .planning/STATE.md: progress 33% → 50%, mark Phase 3 complete, record SHA + timestamp

    ## Pre-flight SQL (paste into Supabase SQL editor for step 4)

    Include in the checklist file as a fenced sql block:
    - Comment header with timestamp expectation
    - INSERT INTO evolution_instances ... for the Fitness UNIC tenant
    - Verification SELECT
    - Rollback DELETE (commented out — operator uncomments only if rolling back)

    ## Rollback procedure (if at any stage things break)

    1. In Evolution dashboard, revert webhook URL to the IARA V2 endpoint immediately
    2. Verify in Supabase: select count(*) from chat_messages where enviada_em > now() - interval '5 minutes' for the UNIC tenant — should stop growing within 30s of the revert
    3. File a STATE.md entry with timestamp and observed failure mode
    4. Open a gap-closure plan for the specific failure
  </action>
  <verify>
    <automated>test -f .planning/phases/03-agente-whatsapp/CUTOVER-CHECKLIST.md && grep -c '\- \[ \]' .planning/phases/03-agente-whatsapp/CUTOVER-CHECKLIST.md | (read n; [ "$n" -ge 12 ] && echo "checklist has $n unchecked items — OK" || (echo "checklist missing items"; exit 1))</automated>
  </verify>
  <done>
    - CUTOVER-CHECKLIST.md exists at the path
    - File contains 13+ unchecked checkbox items spanning Pre-flight → Stage 1 → Stage 2 → Stage 3 → Rollback
    - File includes the pre-flight SQL block for the evolution_instances seed
    - File includes the explicit rollback procedure
    - File does NOT contain the actual EVOLUTION_WEBHOOK_SECRET value (placeholder only — secret stays in Vercel env)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Cutover Stage 1 (10%) — owner gates the first real-lead production traffic</name>
  <what-built>
    - Plans 03-01 and 03-02 deployed to Vercel production
    - Smoke suite green against production URL
    - Identity-leak report semantically approved (Task 3)
    - CUTOVER-CHECKLIST.md gates documented
    - Fitness UNIC evolution_instances row seeded
  </what-built>
  <how-to-verify>
    Walk through the CUTOVER-CHECKLIST.md Pre-flight section (items 1-6) and Stage 1 (items 7-8) in person.
    1. Tick each pre-flight item only after the expected outcome is verified
    2. Switch the Evolution webhook URL to 10% (or single test-lead window if Evolution doesn't support splitting)
    3. Observe for 1 hour
    4. Review the first 5 agent replies in /dashboard/conversas — do they match UNIC voice? Did agendar_aula_experimental fire at least once? Any unexpected handoffs?
    5. Check /api/admin/saude-mkt — is usage on track? Has kill switch fired (unexpected at 10%)?
    If everything is healthy: approve and proceed to Stage 2 manually per checklist.
    If anything broke: rollback per checklist step 7 rollback, file the failure mode in STATE.md, return to revision.
  </how-to-verify>
  <resume-signal>Type "approved-stage-1" to continue, "rollback" + failure description to halt and revise, or "pause-after-10" if you want to leave production at 10% for a multi-day soak before proceeding to Stage 2</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Smoke test runner (operator laptop) → Vercel preview | Smoke tests use service_role key to seed/cleanup; key crosses operator → Supabase admin trust boundary |
| .env.smoke on operator disk | Contains SMOKE_SERVICE_ROLE_KEY and SMOKE_EVOLUTION_WEBHOOK_SECRET — high-value secrets |
| Evolution dashboard → production webhook URL | Cutover step changes routing; misconfiguration could send real-lead traffic to wrong endpoint |
| Fictitious 'Academia Premium Vértice' tenant in production DB | Tenant data lives alongside real-tenant data; cleanup must not accidentally affect real tenants |
| Real Fitness UNIC inbound leads during 10/50/100% cutover | Real PII + real conversion opportunity at stake |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-26 | Information Disclosure | SMOKE_SERVICE_ROLE_KEY in .env.smoke leaked via git | mitigate | .env.smoke in .gitignore (asserted in done criteria); README explicitly warns "NEVER commit"; operator follows CLAUDE.md "Secrets no chat: nunca" rule. |
| T-03-27 | Tampering | cleanupTestTenant called with wrong tenantId, deleting real Fitness UNIC data | mitigate | cleanupTestTenant asserts tenantId !== SMOKE_REAL_PRODUCTION_TENANT_IDS via a hard-coded denylist in seed.ts top-level constant; if assertion fails, throws immediately without DELETE. Test tenants seeded with name prefix 'SMOKE_' for additional sanity check. |
| T-03-28 | Denial of Service | Smoke tests run accidentally against production base URL during CI | mitigate | tests/smoke/README.md states smoke suite is NOT in CI; package.json "test:smoke" script is separate from "test" (CI runs "test" only); no GitHub Actions workflow added in this plan that runs smoke automatically. |
| T-03-29 | Repudiation | Cutover at 100% silently fails for a subset of leads | mitigate | CUTOVER-CHECKLIST stages 7-13 require owner to verify saude-mkt and conversas counts at each stage before advancing; rollback procedure is a single Evolution dashboard step (revert URL); STATE.md entry mandatory on rollback. |
| T-03-30 | Information Disclosure | Smoke test stdout leaking lead conteudo | mitigate | done criteria for Task 1 includes grep over stdout for remotejid/conteudo/telefone returning 0 lines. console.log calls in helpers + tests log only tenantId, messageId, status code, assertion result. |
| T-03-31 | Spoofing | Operator runs smoke tests with valid HMAC against real Fitness UNIC tenant by mistake | mitigate | seedTestTenant returns a NEW tenant_id distinct from real tenants; .env.smoke specifies SMOKE_TEST_TENANT_ID which the test files use exclusively; the production Fitness UNIC tenant is never targeted by smoke tests. Pre-flight SQL in CUTOVER-CHECKLIST is a SEPARATE manual step done in Supabase SQL editor by the operator, not by the smoke runner. |
| T-03-32 | Tampering | Anti-leak gate has false-negative — agent's response sounds "Vértice formal" syntactically but still leaks UNIC modality names | accept-with-review | Hard regex gate (banned coloquial vocab) is automated. Soft semantic gate is owner-reviewed in Task 3 checkpoint. Residual risk: a clever leak the owner misses. Mitigation: re-run the anti-leak gate quarterly and on every system-prompt change (out of Phase 3 scope — added to STATE.md follow-ups). |
| T-03-33 | Elevation of Privilege | RLS inverse smoke test inadvertently grants temporary elevation via misuse of service_role JWT | mitigate | rls-inverse test acquires user session via supabase.auth.signInWithPassword with a pre-seeded test user (not service_role JWT). Service_role is only used in seed/cleanup helpers, never in the assertion phase. |
| T-03-34 | Denial of Service | Kill switch test exhausts OpenAI quota for the test tenant for the rest of the day | mitigate | kill-switch test budget set to 0.01 USD (one tiny turn) then reset to 0.10 in teardown. Test tenant has dedicated ia_limite_diario_usd=0.10. Real OpenAI account quota unaffected. |
| T-03-SC | Tampering | Package installs (vitest, dotenv) | mitigate | vitest by vitest-dev (verified org), dotenv by motdotla — both canonical maintainers per npmjs.com/package/<name>. Treat as [ASSUMED] per RESEARCH.md fallback; operator confirms ownership via npm view <pkg> maintainers before npm install. |
</threat_model>

<verification>
**Phase-level checks before declaring Plan 03-03 complete:**
- `npm run test:smoke` exits 0 (all 8 smoke tests green) against production URL
- Task 3 checkpoint approved (semantic identity-leak review)
- Task 5 checkpoint approved (Stage 1 cutover stable)
- CUTOVER-CHECKLIST.md exists with all required sections
- Fitness UNIC tenant evolution_instances row exists in production with correct webhook_secret
- /api/admin/saude-mkt returns expected payload for the UNIC tenant
- .env.smoke is gitignored; tests/smoke/.identity-leak-report.json is gitignored
- STATE.md updated to reflect Phase 3 completion (after final 100% cutover and Task 5 stage decisions)
</verification>

<success_criteria>
- WHATS-01 verified live: real lead → automated WhatsApp reply within 5 minutes — measured by enviada_em delta in chat_messages for at least 3 production leads during Stage 1 observation window
- WHATS-02 verified live: at least 1 production lead reached status='agendado' via the agendar_aula_experimental tool during Stage 1 or Stage 2
- WHATS-03 verified live: owner used the /dashboard/conversas Assumir button on at least 1 real conversa during Stage 1 or Stage 2 and observed the agent stop replying for that conversa
- ADR §11 smoke suite: all 8 tests (including anti-leak gate hard regex) pass
- Anti-leak gate: semantic review by owner confirmed 0/10 Vértice responses leaked UNIC voice
- Cutover progressed through Stage 1 (10%) under owner observation, with explicit checkpoint approval before each subsequent stage
- Rollback procedure exists and is owner-readable
</success_criteria>

<output>
Create `.planning/phases/03-agente-whatsapp/03-03-SUMMARY.md` when done — include: smoke results, identity-leak report findings, cutover stage reached at time of summary, kill switch behavior observed in test, any production incidents during cutover, follow-up items for Phase 3.1.
</output>
